import FooterTicker from '../../../components/layout/FooterTicker.jsx'

/**
 * Page 1B — the ticker footer band directly beneath the Landing hero,
 * separate from the site-wide Contact Us footer at the bottom of the page.
 */
export default function Footer() {
  return (
    <section className="section-shell">
      <FooterTicker />
    </section>
  )
}